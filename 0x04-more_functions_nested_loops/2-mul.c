#include "main.h"
/**
 * mul - mulitple two numbers
 * @a: parameter number 1
 * @b: parameter number
 * Return: result of multiple of two numbers
 */

int mul(int a, int b)
{
    int result;

    result = a * b;
    return (result);
}
