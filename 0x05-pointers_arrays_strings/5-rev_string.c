#include "main.h"
/**
 * rev_string - Print the string in reverse
 * @s: The string to print in rev
 */
void rev_string(char *s)
{
int a = 0;
int b, i;
char c;

while (s[a] != '\0')
a++;

i = a - 1;

for (b = 0; c >= 0 && b < c; i-- ,b++)
{
c = s[b];
s[b] = s[i];
s[i] = d;
}
}
