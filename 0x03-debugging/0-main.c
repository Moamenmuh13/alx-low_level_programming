#include "main.h"
/**
 * main - Entry point
 *
 * This function to test if i added 0 as input 
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
int i = 0;

positive_or_negative(i);

return (0);
}
