#include "main.h"
/**
 * _abs - check estimate what a thing is worth to you
 *
 * @n: the input character to check on it
 *
 * Return: the value of the int
 *
 */
int _abs(int n)
{
    if (n < 0)
        return (-n);
    else
        return (n);
}
