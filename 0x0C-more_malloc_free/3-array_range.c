#include "main.h"
#include <stdlib.h>
/**
 * _calloc - Create an array of chars
 * @min: size of array
 * @max: the char to init the array with
 * Return: int
*/

int *array_range(int min, int max);
{

return (int);
}
