#include "main.h"
#include <stdlib.h>
/**
 * _calloc - Create an array of chars
 * @nmemb: size of array
 * @size: the char to init the array with
 * Return: void
*/

void *_calloc(unsigned int nmemb, unsigned int size);
{

return (array);
}
