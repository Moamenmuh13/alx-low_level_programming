#include "main.h"
#include <stdlib.h>
/**
 * string_nconcat - Create an array of chars
 * @s1: size of array
 * @s2: the char to init the array with
 * @n: the char to init the array with
 * Return: successful return the array
*/

char *string_nconcat(char *s1, char *s2, unsigned int n)
{

return (char);
}
